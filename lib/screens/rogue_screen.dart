import 'package:flutter/material.dart';

class RogueScreen extends StatefulWidget {
  static const routeName = 'Ladino(a)';

  @override
  _RogueScreenState createState() => _RogueScreenState();
}

class _RogueScreenState extends State<RogueScreen> {
  @override

  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.orangeAccent,
      body: Container(
        child: Column(
          children: <Widget>[

          ],
        ),
      ),
    );
  }
}