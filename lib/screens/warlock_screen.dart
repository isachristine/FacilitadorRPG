import 'package:flutter/material.dart';

class WarlockScreen extends StatefulWidget {
  static const routeName = 'Bruxo(a)';

  @override
  _WarlockScreenState createState() => _WarlockScreenState();
}

class _WarlockScreenState extends State<WarlockScreen> {
  @override

  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.redAccent,
      body: Container(
        child: Column(
          children: <Widget>[

          ],
        ),
      ),
    );
  }
}