import 'package:flutter/material.dart';

class BarberianScreen extends StatefulWidget {
  static const routeName = 'Bárbaro';

  @override
  _BarberianScreenState createState() => _BarberianScreenState();
}

class _BarberianScreenState extends State<BarberianScreen> {
  @override

  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
      body: Container(
        child: Column(
          children: <Widget>[

          ],
        ),
      ),
    );
  }
}